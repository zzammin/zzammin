# 테이블 : 다른 장소 가기, 안주 먹기, 자리에 없는사람 잔 확인(4개의 잔 중 하나만 확인 가능)
# 편의점 : 테이블로 돌아가기, 아이스크림(1턴, 취기 - 포만도 + ), 숙취해소제(2턴, 취기 -- )

import pygame

pygame.init()

screen=pygame.display.set_mode((480,640))
pygame.display.set_caption("chokproject")

